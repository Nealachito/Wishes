import { FiltroOkeyPipe } from './filtro-okey.pipe';

describe('FiltroOkeyPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroOkeyPipe();
    expect(pipe).toBeTruthy();
  });
});
